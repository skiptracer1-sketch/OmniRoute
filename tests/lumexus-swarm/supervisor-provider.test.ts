import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SecuritySwarmRuntime } from '../../src/lumexus-swarm/runtime'; import { SecuritySwarmSupervisor } from '../../src/lumexus-swarm/supervisor'; it('preserves provider per planned role',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const p=await new SecuritySwarmSupervisor(new SecuritySwarmRuntime(k,{route:async()=>({provider:'omniroute',model:'m'})})).plan(m);expect(p.every(x=>x.route.provider==='omniroute')).toBe(true);});
