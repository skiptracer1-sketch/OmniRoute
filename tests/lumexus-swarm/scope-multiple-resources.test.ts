import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('supports multiple explicitly authorized resources',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'a',actions:['inspect']},{resource:'b',actions:['inspect']}]});expect(k.evaluateTool({missionId:m.id,agentId:'r',resource:'b',action:'inspect',risk:'low'}).outcome).toBe('allow');});
