import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('denies out-of-scope critical request rather than creating approval',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['verify']}]});expect(k.evaluateTool({missionId:m.id,agentId:'v',resource:'y',action:'verify',risk:'critical'}).outcome).toBe('deny');expect(k.listApprovals(m.id)).toHaveLength(0);});
