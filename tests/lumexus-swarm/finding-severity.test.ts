import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('preserves finding severity',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});expect(k.recordFinding({missionId:m.id,agentId:'r',title:'x',severity:'critical',evidence:['e']}).severity).toBe('critical');});
