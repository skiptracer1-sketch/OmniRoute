import { expect, it, vi } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway';
it('prevents executor invocation after kill switch',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const execute=vi.fn();k.kill();await expect(new SwarmToolGateway(k,{execute}).execute({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low'})).rejects.toThrow('kill switch');expect(execute).not.toHaveBeenCalled();});
