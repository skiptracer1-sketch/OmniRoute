import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('preserves mission id in approval record',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['verify']}]});k.evaluateTool({missionId:m.id,agentId:'v',resource:'x',action:'verify',risk:'high'});expect(k.listApprovals(m.id)[0]?.missionId).toBe(m.id);});
