import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway'; it('executes authorized medium risk operation',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const r=await new SwarmToolGateway(k,{execute:async()=>42}).execute({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'medium'});expect(r).toEqual({status:'executed',output:42});});
