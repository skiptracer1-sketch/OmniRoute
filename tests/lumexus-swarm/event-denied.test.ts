import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('audits denied requests',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.evaluateTool({missionId:m.id,agentId:'r',resource:'y',action:'inspect',risk:'low'});expect(k.listEvents(m.id).at(-1)?.type).toBe('tool.denied');});
