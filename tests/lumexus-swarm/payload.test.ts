import { expect, it, vi } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway';
it('passes payload to executor after policy allow', async()=>{ const k=new LumexusSwarmKernel(); const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]}); const execute=vi.fn().mockResolvedValue('ok'); await new SwarmToolGateway(k,{execute}).execute({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low',payload:{q:1}}); expect(execute).toHaveBeenCalledWith({resource:'x',action:'inspect',payload:{q:1}}); });
