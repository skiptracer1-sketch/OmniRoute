import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('counts multiple pending approvals',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['verify']}]});for(let i=0;i<2;i++)k.evaluateTool({missionId:m.id,agentId:`v${i}`,resource:'x',action:'verify',risk:'high'});expect(k.pulse(m.id).approvalsPending).toBe(2);});
