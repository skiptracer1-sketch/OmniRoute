import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('counts multiple mission findings',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});for(let i=0;i<2;i++)k.recordFinding({missionId:m.id,agentId:'r',title:`x${i}`,severity:'low',evidence:['e']});expect(k.pulse(m.id).findings).toBe(2);});
