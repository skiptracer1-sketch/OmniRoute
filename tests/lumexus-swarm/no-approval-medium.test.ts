import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('does not create approval for medium-risk authorized request',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.evaluateTool({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'medium'});expect(k.listApprovals(m.id)).toHaveLength(0);});
