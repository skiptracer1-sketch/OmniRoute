import { expect, it, vi } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway';
it('does not forward payload for approval-gated requests', async()=>{ const k=new LumexusSwarmKernel(); const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['verify']}]}); const execute=vi.fn(); const r=await new SwarmToolGateway(k,{execute}).execute({missionId:m.id,agentId:'v',resource:'x',action:'verify',risk:'high',payload:{secret:'not-forwarded'}}); expect(r.status).toBe('approval-required'); expect(execute).not.toHaveBeenCalled(); });
