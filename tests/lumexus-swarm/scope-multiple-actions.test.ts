import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('supports explicitly scoped multiple actions',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect','report']}]});expect(k.evaluateTool({missionId:m.id,agentId:'report',resource:'x',action:'report',risk:'low'}).outcome).toBe('allow');});
