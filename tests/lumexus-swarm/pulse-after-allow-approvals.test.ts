import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('does not create approval for allowed request',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.evaluateTool({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low'});expect(k.pulse(m.id).approvalsPending).toBe(0);});
