import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('denied request preserves created state before start',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.evaluateTool({missionId:m.id,agentId:'r',resource:'y',action:'inspect',risk:'low'});expect(k.pulse(m.id).status).toBe('created');});
