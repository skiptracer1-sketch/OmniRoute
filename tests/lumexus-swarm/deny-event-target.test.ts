import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('includes denied resource and action in audit event',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.evaluateTool({missionId:m.id,agentId:'r',resource:'y',action:'verify',risk:'low'});expect(k.listEvents(m.id).at(-1)?.payload).toEqual({resource:'y',action:'verify'});});
