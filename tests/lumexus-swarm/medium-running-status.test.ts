import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('authorized medium risk preserves running state',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.startMission(m.id);k.evaluateTool({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'medium'});expect(k.pulse(m.id).status).toBe('running');});
