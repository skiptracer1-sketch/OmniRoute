import { expect, it, vi } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SecuritySwarmRuntime } from '../../src/lumexus-swarm/runtime';
it('passes agent identity to OmniRoute',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const route=vi.fn().mockResolvedValue({provider:'x',model:'y'});await new SecuritySwarmRuntime(k,{route}).routeAgent(m,'agent-42','recon');expect(route).toHaveBeenCalledWith(expect.objectContaining({agentId:'agent-42'}));});
