import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('explains allowed request',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});expect(k.evaluateTool({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low'}).reason).toContain('in scope');});
