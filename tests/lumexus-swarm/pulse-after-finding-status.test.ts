import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('retains running status when recording a finding',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.startMission(m.id);k.recordFinding({missionId:m.id,agentId:'r',title:'x',severity:'low',evidence:['e']});expect(k.pulse(m.id).status).toBe('running');});
