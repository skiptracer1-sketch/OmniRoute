import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SecuritySwarmRuntime } from '../../src/lumexus-swarm/runtime'; import { SecuritySwarmSupervisor } from '../../src/lumexus-swarm/supervisor'; import { SECURITY_ROLES } from '../../src/lumexus-swarm/types';
it('plans roles in deterministic registry order',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const p=await new SecuritySwarmSupervisor(new SecuritySwarmRuntime(k,{route:async()=>({provider:'x',model:'y'})})).plan(m);expect(p.map(x=>x.role)).toEqual(SECURITY_ROLES);});
