import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway';
it('surfaces executor failure after policy allow',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const g=new SwarmToolGateway(k,{execute:async()=>{throw new Error('executor failed')}});await expect(g.execute({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low'})).rejects.toThrow('executor failed');});
