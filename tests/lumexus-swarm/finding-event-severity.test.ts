import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('includes finding severity in audit event',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.recordFinding({missionId:m.id,agentId:'r',title:'x',severity:'critical',evidence:['e']});expect(k.listEvents(m.id).at(-1)?.payload).toEqual(expect.objectContaining({severity:'critical'}));});
