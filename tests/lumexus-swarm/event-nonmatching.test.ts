import { expect, it, vi } from 'vitest'; import { SwarmEventFabric } from '../../src/lumexus-swarm/events'; it('does not call subscriber for nonmatching event',()=>{const f=new SwarmEventFabric();const h=vi.fn();f.subscribe('finding.recorded',h);f.publish({id:'e',missionId:'m',type:'mission.started',timestamp:new Date().toISOString(),payload:{}});expect(h).not.toHaveBeenCalled();});
