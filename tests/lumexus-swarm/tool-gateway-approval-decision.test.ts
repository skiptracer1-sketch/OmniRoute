import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway'; it('returns approval-required decision without execution',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['verify']}]});const r=await new SwarmToolGateway(k,{execute:async()=>null}).execute({missionId:m.id,agentId:'v',resource:'x',action:'verify',risk:'high'});expect(r.status).toBe('approval-required');if(r.status==='approval-required')expect(r.decision.outcome).toBe('approval-required');});
