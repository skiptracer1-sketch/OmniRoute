import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SecuritySwarmRuntime } from '../../src/lumexus-swarm/runtime'; import { SecuritySwarmSupervisor } from '../../src/lumexus-swarm/supervisor';
it('surfaces supervisor routing failure',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const s=new SecuritySwarmSupervisor(new SecuritySwarmRuntime(k,{route:async()=>{throw new Error('route failed')}}));await expect(s.plan(m)).rejects.toThrow('route failed');});
