import { expect, it, vi } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SecuritySwarmRuntime } from '../../src/lumexus-swarm/runtime';
it('passes mission objective to OmniRoute',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'authorized inspection',scope:[{resource:'x',actions:['inspect']}]});const route=vi.fn().mockResolvedValue({provider:'x',model:'y'});await new SecuritySwarmRuntime(k,{route}).routeAgent(m,'a','recon');expect(route).toHaveBeenCalledWith(expect.objectContaining({objective:'authorized inspection'}));});
