import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; it('kill switch takes precedence over otherwise allowed request',()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});k.kill();expect(()=>k.evaluateTool({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low'})).toThrow('kill switch');});
