import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway';
it('returns output only after an allowed execution', async()=>{ const k=new LumexusSwarmKernel(); const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]}); const r=await new SwarmToolGateway(k,{execute:async()=>({safe:true})}).execute({missionId:m.id,agentId:'r',resource:'x',action:'inspect',risk:'low'}); expect(r).toEqual({status:'executed',output:{safe:true}}); });
