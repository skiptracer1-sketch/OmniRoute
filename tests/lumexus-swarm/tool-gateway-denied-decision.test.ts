import { expect, it } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SwarmToolGateway } from '../../src/lumexus-swarm/tool-gateway'; it('returns denial decision without execution',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const r=await new SwarmToolGateway(k,{execute:async()=>null}).execute({missionId:m.id,agentId:'r',resource:'y',action:'inspect',risk:'low'});expect(r.status).toBe('denied');if(r.status==='denied')expect(r.decision.outcome).toBe('deny');});
