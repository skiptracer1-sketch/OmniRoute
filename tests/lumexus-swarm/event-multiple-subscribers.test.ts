import { expect, it, vi } from 'vitest'; import { SwarmEventFabric } from '../../src/lumexus-swarm/events'; it('notifies multiple matching subscribers',()=>{const f=new SwarmEventFabric();const a=vi.fn(),b=vi.fn();f.subscribe('mission.started',a);f.subscribe('mission.started',b);f.publish({id:'e',missionId:'m',type:'mission.started',timestamp:new Date().toISOString(),payload:{}});expect(a).toHaveBeenCalledOnce();expect(b).toHaveBeenCalledOnce();});
