import { expect, it, vi } from 'vitest'; import { LumexusSwarmKernel } from '../../src/lumexus-swarm/kernel'; import { SecuritySwarmRuntime } from '../../src/lumexus-swarm/runtime'; it('preserves mission id in routed task',async()=>{const k=new LumexusSwarmKernel();const m=k.createMission({name:'x',objective:'x',scope:[{resource:'x',actions:['inspect']}]});const route=vi.fn().mockResolvedValue({provider:'x',model:'y'});await new SecuritySwarmRuntime(k,{route}).routeAgent(m,'a','recon');expect(route).toHaveBeenCalledWith(expect.objectContaining({missionId:m.id}));});
